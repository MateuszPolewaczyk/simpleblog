{% extends 'base.html.twig' %}

{% block body %}
	<div>
		<ul>
			{% for post in posts %}
	    		<li>
	    			<a href="{{ path('showPost', {id: post.id}) }}">
	    				{{ post.title }}
	    			</a>
	    		</li>
			{% endfor %}
		</ul>
	</div>
{% endblock %}
